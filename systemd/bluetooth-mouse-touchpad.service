[Unit]
Description=Disable the trackpad automatically while a Bluetooth mouse is connected

[Service]
ExecStart=%h/.local/bin/bluetooth-mouse-touchpad
Restart=always

[Install]
WantedBy=graphical-session.target
